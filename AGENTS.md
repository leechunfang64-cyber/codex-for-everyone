# AGENTS.md

## 项目目标

Codex for Everyone 是一本面向真实开发场景的 OpenAI Codex 开源实战手册。写作目标不是制造 AI 神话，而是把真实项目、真实排障、真实提示词和产品化路径沉淀成长期资产。

## 沟通与写作风格

- 中文为主，必要时保留英文术语。
- 直接、务实、可执行，不写空泛的 AI 口号。
- 每个判断尽量回到需求、渠道、交付、验证、成本、风险、复用和现金流。
- 不把 Codex 描述成魔法。核心方法是需求拆解、上下文供给、反馈迭代、验证闭环。
- 遇到工具、模型、平台能力变化时，以官方文档和当前验证结果为准。

## 内容要求

实战案例必须尽量包含：

- 适用场景
- 需求拆解
- Codex 提示词
- 实现步骤
- 验证方式
- 常见坑
- 可复用资产
- 产品化判断

排障文章必须尽量包含：

- 现象
- 判断路径
- 命令
- 预期输出
- 错误分叉
- 不要做什么

## 安全边界

- 不提交 API Key、token、cookie、真实客户数据、内部账号、付款信息。
- 涉及成本、收入、部署用量时，不使用无法验证的夸张数字。
- 涉及安全补救时，优先写清楚撤销、轮换、审计和最小权限。

## 工程约定

- VitePress 以仓库根目录作为 source root。
- 本地开发使用 `npm run dev`。
- 静态构建使用 `npm run build`。
- Cloudflare Pages 输出目录是 `.vitepress/dist`。
- 新增文章时同步更新 `.vitepress/config.mts` 的侧边栏。
